// 初期データの定義。
//
// ラウンド履歴は含めない。
// 公開URLに個人のスコア履歴を置くと、URLを知っている人全員が見られるため、
// 各利用者が自分の端末で入力するか、バックアップ（JSON）から取り込む方式にしている。
// 過去の記録は分析画面の「JSONを読み込む」から復元できる。

/** 番手の一覧（要件6.3）。距離は各自が実測して登録する。 */
export const CLUBS = ['Driver', '4UT', '5UT', '6I', '7I', '8I', '9I', 'PW', '48°', '52°', '56°'];

/** キャリー表の初期状態。距離は空にしておき、実測値を入れてもらう。 */
export const SEED_CARRY = CLUBS.map((club) => ({
  club,
  normalCarry: null,
  safeCarry: null,
}));

/** 初期ラウンドは持たない（各自の入力かバックアップの取り込みで作る） */
export const SEED_ROUNDS = [];
